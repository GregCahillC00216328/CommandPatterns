#include "InputHandler.h"
#include <SFML/Graphics.hpp>



void InputHandler::handleInput()
{
	/*if (isPressed(buttonX_)) buttonX_->execute();
	else if (isPressed(buttonY_)) buttonY_->execute();
	else if isPressed(buttonA_)*/
		
}